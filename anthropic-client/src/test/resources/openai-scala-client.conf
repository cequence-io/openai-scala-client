# Test-only subset of openai-client's openai-scala-client.conf: the anthropic-client module
# depends only on openai-core, so the real mapping (shipped with openai-client) is not on this
# module's test classpath.
openai-scala-client {
  reasoning-effort-thinking-budget-mapping {
    medium {
      anthropic = 4096
    }
  }
}
