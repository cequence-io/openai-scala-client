validateWithChatGPT = false

openai-scala-client {
    apiKey = ${?OPENAI_SCALA_CLIENT_API_KEY}
    orgId = ${?OPENAI_SCALA_CLIENT_ORG_ID}

    timeouts {
        requestTimeoutSec = 200
        readTimeoutSec = 200
    }
}